# workflows

This folder is intentionally created to hold GitHub Actions workflow files (e.g., `.github/workflows/*` or CI/CD definitions).

Placeholder README to ensure the `workflows/` directory exists in the repository.
